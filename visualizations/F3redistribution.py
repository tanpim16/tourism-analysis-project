import pandas as pd import matplotlib.pyplot as plt from matplotlib.ticker import PercentFormatter from matplotlib.gridspec import GridSpec import os # ─── Style ──────────────────────────────────────────────────────────────────── plt.rcParams.update({ 'figure.facecolor': 'white', 'axes.facecolor': '#F8F9FA', 'axes.grid': True, 'grid.color': '#FFFFFF', 'grid.linewidth': 1.2, 'font.family': 'sans-serif', 'axes.spines.top': False, 'axes.spines.right': False, 'axes.spines.left': False, 'axes.spines.bottom':False, }) COLORS = { 'Major City': '#1B4F8A', 'Secondary City': '#16A085', } # ─── Load Data ──────────────────────────────────────────────────────────────── data_path = "data/processed/master_tourism_analysis.csv" df = pd.read_csv(data_path) # Convert Thai BE → CE df['Year_CE'] = df['Year'].astype(int) - 543 df['date'] = pd.to_datetime( df['Year_CE'].astype(str) + '-' + df['Month'], format='%Y-%b' ) # ─── Redistribution Calculation ─────────────────────────────────────────────── df_share = ( df.groupby(['date','City_type_EN'])['total_visitors'] .sum() .reset_index() ) # total visitors per month df_share['total_all'] = df_share.groupby('date')['total_visitors'].transform('sum') # share (0–1) df_share['visitor_share'] = df_share['total_visitors'] / df_share['total_all'] # filter study period df_share = df_share.query("'2023-01-01' <= date <= '2025-12-01'") # ─── Prepare stacked structure ──────────────────────────────────────────────── pivot = ( df_share.pivot(index='date', columns='City_type_EN', values='visitor_share') .sort_index() ) dates = pivot.index major = pivot['Major City'] secondary = pivot['Secondary City'] os.makedirs('visualizations', exist_ok=True) # ─── Layout (Header + Chart Area) ───────────────────────────────────────────── fig = plt.figure(figsize=(16,8)) gs = GridSpec( 2, 1, height_ratios=[0.18, 0.82], figure=fig ) ax = fig.add_subplot(gs[1]) # ─── 100% Stacked Area Chart ────────────────────────────────────────────────── ax.stackplot( dates, major, secondary, colors=[COLORS['Major City'], COLORS['Secondary City']], alpha=0.8, labels=['Major City', 'Secondary City'] ) # boundary lines for clarity ax.plot(dates, major, color='white', linewidth=1.5) ax.plot(dates, major + secondary, color='#333', linewidth=1) # ─── Axis Formatting ────────────────────────────────────────────────────────── ax.set_ylim(0, 1) ax.yaxis.set_major_formatter(PercentFormatter(1.0)) ax.set_ylabel("Visitor Share") ticks = pd.date_range('2023-01-01','2026-01-01',freq='QS-JAN') ax.set_xticks(ticks) ax.set_xticklabels([d.strftime('%b\n%Y') for d in ticks]) ax.set_xlim(pd.Timestamp('2022-12-01'), pd.Timestamp('2025-12-31')) ax.margins(x=0.01) # ─── Header (NO OVERLAP) ────────────────────────────────────────────────────── fig.text( 0.07, 0.93, "Tourism Redistribution Share", fontsize=20, fontweight='bold', color='#1A1A2E' ) fig.text( 0.07, 0.895, "100% stacked distribution of visitors by city type · Jan 2023 – Dec 2025", fontsize=11, color='#888' ) # ─── Legend in header zone ───────────────────────────────────────────────────── ax.legend( loc='upper left', bbox_to_anchor=(0, 1.08), ncol=2, framealpha=0.9 ) # ─── Source ─────────────────────────────────────────────────────────────────── fig.text( 0.07, 0.04, "Source: Thailand Tourism Authority", fontsize=8.5, color='#AAA' ) # ─── Save ───────────────────────────────────────────────────────────────────── plt.savefig( "visualizations/Figure 3.png", dpi=300, bbox_inches='tight', facecolor='white' ) plt.close() print("✅ Figure 3 (100% stacked area) created successfully") from this how to shorter the margin of the empty space